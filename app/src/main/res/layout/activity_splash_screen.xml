<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/colorPrimary"
    android:gravity="center"
    android:padding="10dp"
    android:id="@+id/transitions_container"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/logo_image_view"
        android:layout_width="250dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_logo"/>

    <RelativeLayout
        android:id="@+id/language_spinner_next_layout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:layout_weight="2"
        android:visibility="gone">

        <LinearLayout
            android:id="@+id/language_spinner_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:orientation="vertical">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/choose_language"
                android:textColor="@android:color/black"
                android:textStyle="bold"
                android:layout_marginTop="20dp"
                android:textSize="@dimen/listing_text_size"/>
            <Spinner
                android:id="@+id/splash_language_spinner"
                android:layout_marginTop="10dp"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:background="@drawable/round_rect_shape"
                android:popupTheme="@style/SpinnerDropdownTheme"
                />
        </LinearLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/language_spinner_layout">
            <android.support.design.widget.FloatingActionButton
                android:id="@+id/splash_start_btn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:fabSize="normal"
                android:layout_centerInParent="true"
                android:src="@drawable/ic_next"
                app:borderWidth="0dp"
                app:elevation="6dp"
                android:backgroundTint="@color/shittyGreen"
                />
        </RelativeLayout>
    </RelativeLayout>

</LinearLayout>